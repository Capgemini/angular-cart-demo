mongo:
  image: 'mongo:latest'
  ports:
    - '27017:27017'
  restart: always
web:
  image: 'tutum.co/tayzlor/angular-cart-demo:latest'
  environment:
    - NODE_ENV=production
    - seedDB=yes
  links:
    - mongo
  ports:
    - '80:8080'
  restart: always
  autoredeploy: true
